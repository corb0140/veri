<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BiometricVault" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!--  Color  -->
    <color name="fab_background">@color/white</color>
    <color name="fab_icon">@color/dark</color>

</resources>
