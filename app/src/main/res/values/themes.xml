<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BiometricVault" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/dark</item>
        <item name="android:windowBackground">?attr/colorSurface</item>

        <item name="fontFamily">@font/roboto</item>
        <item name="android:fontFamily">@font/roboto</item>

    </style>

    <style name="Theme.BiometricVault" parent="Base.Theme.BiometricVault" />

    <style name="Theme.BiometricVault.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.BiometricVault</item>
    </style>
</resources>